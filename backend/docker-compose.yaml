# docker compose for python backend and nextjs frontend
version: '3.8'
services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "80:3000"
    links:
    - backend
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    environment:
      DEPLOYMENT: REMOTE